# MySQL 配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456
DB_NAME=todos_db

# MongoDB 配置
MONGO_URI=mongodb://localhost:27017/todos_db
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DB_NAME=todos_db

# 数据库读写配置
# 读取策略: mysql | mongo | prefer-mongo
DB_READ_FROM=mysql
# 写入模式: mysql | dual | mongo
DB_WRITE_MODE=mysql

# JWT 配置
JWT_SECRET=your_jwt_secret_key

# 推送通知配置
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=